import { SafeAreaView, ScrollView, View, Text } from "react-native";

export default function OrderScreen() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>Order</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}
